language: node_js

node_js:
  - "stable"

cache:
  directories:
    - ./client/node_modules
      ./server/node_modules

before_script:
  - yarn run installAll

script:
  - yarn --cwd ./client run build
